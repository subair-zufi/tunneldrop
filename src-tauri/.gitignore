/target/
/gen/schemas
/binaries/
debug.log

# The bundled cloudflared for Android: ~28 MB per ABI, produced on demand by
# scripts/build-cloudflared-android.sh. Same treatment as the desktop sidecar
# in /binaries/ — fetched at build time, never committed.
/gen/android/app/src/main/jniLibs/
